x-common-env: &common-env
  env_file: ../.env

x-worker-base: &worker-base
  restart: unless-stopped
  env_file: ../.env
  networks:
    - lead-net
  depends_on:
    postgres:
      condition: service_healthy
    rabbitmq:
      condition: service_healthy

services:
  # ── Infrastructure ─────────────────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-lead_generator}
      POSTGRES_USER: ${POSTGRES_USER:-lead_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lead_secret}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    networks:
      - lead-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lead_user} -d ${POSTGRES_DB:-lead_generator}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    ports:
      - "5672:5672"
      - "15672:15672"   # Management UI
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - lead-net
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── Workers ────────────────────────────────────────────────────────────────
  scraper:
    <<: *worker-base
    build:
      context: ..
      dockerfile: services/scraper/Dockerfile
    container_name: lead-scraper

  validator:
    <<: *worker-base
    build:
      context: ..
      dockerfile: services/validator/Dockerfile
    container_name: lead-validator

  deduplicator:
    <<: *worker-base
    build:
      context: ..
      dockerfile: services/deduplicator/Dockerfile
    container_name: lead-deduplicator

  scorer:
    <<: *worker-base
    build:
      context: ..
      dockerfile: services/scorer/Dockerfile
    container_name: lead-scorer

  distributor:
    <<: *worker-base
    build:
      context: ..
      dockerfile: services/distributor/Dockerfile
    container_name: lead-distributor

networks:
  lead-net:
    driver: bridge

volumes:
  postgres-data:
  rabbitmq-data:
